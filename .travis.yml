language: node_js
node_js: lts/*

env:
  - TZ='Europe/Paris'

services:
  - docker

before_install:
  - docker-compose --version
  - docker-compose up -d
  - docker ps

script:
  - commitlint-travis
  - yarn lint
  - sleep 20; yarn test --forceExit
